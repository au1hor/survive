using UnityEngine;

public class PlayerAnimations : MonoBehaviour
{
    public SpriteRenderer playerSpr;
    public Sprite[] sprs;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void changeSprByMove()
    {
        
    }
}
